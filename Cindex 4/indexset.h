#pragma once
short is_setstruct(HWND hwnd, INDEXPARAMS *idxptr);		/* sets up new record struct */
void is_setrefsyntax(HWND hwnd);	/* verifies cross-refs */
void is_flipwords(HWND hwnd);	// sets up flipped words
